import styled from 'styled-components'

export const NavigationWrapper = styled.nav``

export const NavigationWrapperContent = styled.ul`
  display: flex;
  align-items: center;

  list-style: none;
`

export const NavigationWrapperContentList = styled.li`
  margin: 0 0px 0 30px;
  display: inline-block;

  a {
    color: #fff;
    text-decoration: none;
    font-family: montserrat;
    font-weight: 600;
    position: relative;
    overflow: hidden;
    display: inline-block;
  }
`
